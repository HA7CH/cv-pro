NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# server-side only, do not expose
# Used by src/lib/supabase/server.ts for all write paths (resume / variants /
# PAT tokens). Bypasses RLS — never ship this to the client bundle.
SUPABASE_SERVICE_ROLE_KEY=

PAT_HASH_SECRET=
